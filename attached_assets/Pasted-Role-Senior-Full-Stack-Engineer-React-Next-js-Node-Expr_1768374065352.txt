Role: Senior Full-Stack Engineer (React / Next.js / Node / Express / Django / Firebase / SQL / MongoDB)
Goal: Fix issue where orders and patient appointments are saved correctly and visible in user sections but NOT appearing in the admin dashboard.

🧠 Problem Description

Orders are visible in User Orders section

Patient appointments are visible in Patient Appointments section

Admin dashboard is empty or partially empty

Data exists in the database but is not fetched, filtered, or rendered correctly for admin

✅ Tasks to Perform
1️⃣ Verify Database Writes

Confirm that:

Orders

Appointments
are being saved in the same database / collection / table used by admin

Check:

Collection names

Table names

Environment (prod vs dev)

console.log("Order saved:", orderData);
console.log("Appointment saved:", appointmentData);

2️⃣ Check Role-Based Filtering

Ensure admin queries are NOT filtering by userId

Admin should fetch ALL records

❌ WRONG (user-only query):

Order.find({ userId: req.user.id })


✅ CORRECT (admin query):

Order.find({})


or

SELECT * FROM orders;

3️⃣ Validate Admin API Endpoints

Ensure admin dashboard uses correct API endpoints

Example:

GET /api/admin/orders
GET /api/admin/appointments


Backend must NOT depend on session userId.

if (req.user.role !== "admin") {
  return res.status(403).json({ message: "Access denied" });
}

4️⃣ Check Authentication & Admin Role

Verify:

role: "admin" exists in database

Token/session includes role

Admin middleware is not blocking valid data

console.log("Admin user:", req.user);

5️⃣ Fix Frontend Fetch Logic

Ensure admin dashboard calls admin APIs, not user APIs

❌ WRONG:

fetch("/api/orders")


✅ CORRECT:

fetch("/api/admin/orders")

6️⃣ Verify Response Rendering

Ensure frontend is mapping correct keys

setOrders(response.data.orders);
setAppointments(response.data.appointments);


❌ Common bug:

response.data.data   // but backend returns `orders`

7️⃣ Check Database Indexes & Status Filters

Admin dashboards often filter by:

status

isActive

isDeleted

Temporarily remove filters to debug:

Order.find({}) // No filters

8️⃣ Fix Real-Time Sync (if using Firebase)

Ensure admin dashboard listens to full collection, not user scoped path

❌

/orders/{userId}


✅

/orders/

9️⃣ Add Logging for Admin Fetch
console.log("Admin orders fetched:", orders.length);
console.log("Admin appointments fetched:", appointments.length);

🛠 Expected Outcome

All orders appear in admin dashboard

All patient appointments appear in admin dashboard

No role-based filtering issues

Frontend correctly renders admin data

Admin dashboard stays in sync with user actions

🚀 Final Instruction to Replit

Debug backend queries, admin APIs, role-based access control, and frontend rendering logic.
Ensure admin dashboard fetches ALL orders and appointments without user-scoped filters and displays them correctly.

If you want, tell me:

Tech stack (Next.js / MERN / Django / Firebase)

Database (MongoDB / PostgreSQL / Firebase)

Auth method (JWT / Clerk / Firebase Auth)