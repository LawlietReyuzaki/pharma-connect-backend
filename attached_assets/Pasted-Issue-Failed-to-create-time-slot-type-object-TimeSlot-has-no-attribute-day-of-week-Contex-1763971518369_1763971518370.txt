Issue: "Failed to create time slot: type object 'TimeSlot' has no attribute 'day_of_week'"

Context:
- Our Replit website's /slash admin time-slot creation fails with above error when creating a TimeSlot for any doctor.
- Likely stack: Python backend (could be Django ORM or SQLAlchemy) — but fix must handle other patterns too.
- Goal: Diagnose root cause, fix model/serialization/creation logic, add tests, and provide a short migration / deploy guide.

Task (do this automatically and return code diffs + short explanation):
1. Search the repository for any `TimeSlot` definitions and all usages:
   - Files to check: `models.py`, `schemas.py`, `serializers.py`, `views.py`, `controllers`, `services`, and migration files.
   - Also search for `day_of_week`, `weekday`, `day`, `dayName` to find naming mismatches.
2. Identify the root cause from these possibilities (report which one applies):
   A. Model missing the `day_of_week` field (e.g., Django model has no such field).
   B. Field exists but name mismatch (e.g., field named `weekday` or `day` instead of `day_of_week`).
   C. Using the class `TimeSlot` instead of an instance when creating (`TimeSlot.day_of_week` vs `instance.day_of_week`).
   D. Serializer / schema does not include `day_of_week`.
   E. Migration not applied (field added but DB not migrated).
   F. Name collision: another `TimeSlot` class imported from different module.
3. Apply the fix based on the cause:
   - If (A) or (B): Add/rename field in the model. Example for Django:
     ```py
     # models.py
     class TimeSlot(models.Model):
         DOCTOR = models.ForeignKey(Doctor, on_delete=models.CASCADE)
         day_of_week = models.IntegerField(choices=[
             (0,'Monday'),(1,'Tuesday'),(2,'Wednesday'),
             (3,'Thursday'),(4,'Friday'),(5,'Saturday'),(6,'Sunday')
         ], null=False)
         start_time = models.TimeField()
         end_time = models.TimeField()
         # other fields...
     ```
     Then run:
     ```
     python manage.py makemigrations
     python manage.py migrate
     ```
   - If (C): Fix creation code to use an instance:
     ```py
     # wrong
     TimeSlot.day_of_week = payload['day_of_week']   # <-- using class

     # correct
     timeslot = TimeSlot.objects.create(
         doctor=doctor_instance,
         day_of_week=payload['day_of_week'],
         start_time=payload['start_time'],
         end_time=payload['end_time']
     )
     ```
   - If (D): Update serializer/schema (Django REST Framework example):
     ```py
     class TimeSlotSerializer(serializers.ModelSerializer):
         class Meta:
             model = TimeSlot
             fields = ['id','doctor','day_of_week','start_time','end_time']
     ```
   - If (E): Add migration & apply; if Replit uses container, include instructions to run migrations during deploy.
   - If (F): Fix imports (use absolute imports) and remove ambiguous `from ... import *`. Ensure `from app.models import TimeSlot` references correct class.
4. Add validation & helpful error message:
   - Validate `day_of_week` is in 0..6 (or allowed strings).
   - Return HTTP 400 with message if invalid.
5. Add tests:
   - Unit test for creating a TimeSlot with valid payload.
   - Test that missing `day_of_week` returns clear error.
6. Provide a short patch (diff) for all files changed and a 1-paragraph explanation of the fix and how to deploy on Replit (include migration command).
7. If backend is NOT Django, provide corresponding SQLAlchemy/Pydantic snippets:
   - SQLAlchemy:
     ```py
     class TimeSlot(Base):
         __tablename__ = 'time_slots'
         id = Column(Integer, primary_key=True)
         doctor_id = Column(Integer, ForeignKey('doctors.id'))
         day_of_week = Column(Integer, nullable=False)
         start_time = Column(Time, nullable=False)
         end_time = Column(Time, nullable=False)
     ```
   - Pydantic/fastapi: ensure schema includes `day_of_week: int`.
8. Finally: list 5 quick checks I can run locally on Replit to confirm the fix (one-line commands).
