Fix the issue where the system generates a valid Google Meet link but does NOT schedule the Google Calendar event correctly.

You must update the appointment booking flow so that all functions are executed in the correct order and verified.

🚀 Requirements:

1. For every new appointment:
   - Generate a unique Google Meet link using Google Calendar API:
     - Use conferenceData.createRequest with type="hangoutsMeet".
     - Ensure the event is created with "conferenceDataVersion": 1.
   - Include the meet link in the Google Calendar event.
   - Add the customer’s email under "attendees" with "email": <customer_email>.
   - Make sure the user creating the event is the organizer.

2. Schedule the Google Calendar event automatically:
   - Use the correct appointment date, start time, and duration.
   - Include the following event fields:
     * summary → Appointment Title (e.g. "Consultation with {{customer_name}}")
     * description → Optional details
     * start → {"dateTime": "<ISO format>", "timeZone": "UTC or local TZ"}
     * end → {"dateTime": "<ISO format + duration>", "timeZone": "UTC or local TZ"}
     * attendees → [{"email": "<customer_email>", "responseStatus": "needsAction"}]
     * conferenceData:
       {
         "createRequest": {
           "requestId": "meet-" + uniqueID,
           "conferenceSolutionKey": {"type": "hangoutsMeet"}
         }
       }

3. Include the Meet link in the confirmation email:
   - Format clearly: "Google Meet Link: {{meet_link}}"
   - Verify the email uses the same meet link generated by the API response.
   - DO NOT reuse old or hardcoded links.

4. Authenticate correctly:
   - Ensure OAuth2 credentials OR service account credentials with domain delegation are properly configured.
   - Ensure `https://www.googleapis.com/auth/calendar.events` and `calendar.events.insert` scopes are enabled.
   - If using service account, ensure impersonation (subject=admin_email) is properly set.

5. Test with multiple appointments:
   - Different emails, times, and titles.
   - Confirm:
     * Calendar event is created
     * Meet link works
     * Invite email is received via Google
     * Confirmation email includes correct info

6. Log and return key data after event creation:
   - event_id
   - meet_link (from event.conferenceData.entryPoints)
   - attendee email
   - start date/time

🛑 Fix all API, OAuth, and endpoint issues preventing event creation.
🛑 Do NOT skip calendar event creation even if meet link is generated.
🛑 Do NOT use static or regenerated links after event creation.

Only modify the appointment booking / email & calendar integration logic.

When finished, provide the updated function responsible for:
- Generating the meet link
- Creating the calendar event
- Adding the invitee
- Returning the meet link and event ID

Make sure to use async/await if required and handle API errors properly.
Provide a complete working example (including Google API setup code).
